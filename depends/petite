#!/bin/bash

MACHINE=`uname`
VER=7.9.4

MT=`$WAVESCRIPTD/depends/get_machine_type`

PATH=depends/petite_chez_scheme$VER
#PATH=depends/csv7.9.4/

SCHEME=scheme
if [ "$MT" == "i3nt" ]; then SCHEME=petite.exe; fi

($WAVESCRIPTD/$PATH/bin/$MT/$SCHEME -b $WAVESCRIPTD/$PATH/boot/$MT/petite.boot $*)
