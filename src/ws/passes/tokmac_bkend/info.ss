(module info (lib "infotab.ss" "setup")
  (define name "Compiler Passes")
  (define compile-omit-files '(
			       "desugar-gradients_ETX.ss"
			       "desugar-gradients_simple.ss"
			       "desugar-gradients_verbose.ss"
			       ))

  )
