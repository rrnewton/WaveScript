;; Pass 29: Inline Tokens

;; [2005.10.26] Gotta do this at some point.  The output of
;; deglobalize is *begging* for it.

;; ================================================================================


(define inline-tokens
  (let ()



    (lambda (prog)
      )))
