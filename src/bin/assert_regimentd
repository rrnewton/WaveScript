#!/bin/bash

if [ ! -d "$WAVESCRIPTD" ];
then 
  DIR=`dirname $0`
  export WAVESCRIPTD=`$DIR/find_regimentd`
  CUR=`pwd`
  if [ -d "$WAVESCRIPTD" ];
  then cd "$WAVESCRIPTD"
       source ./install_environment_vars
       cd "$CUR";
  fi
fi
